include:
  # repo: https://cd.splunkdev.com/core-ee/signing/api-integration/-/tree/develop/
  - project: core-ee/signing/api-integration
    ref: develop
    file: /templates/.sign-client.yml

stages:
  - build
  - sign
  - checksum

linux-build:
  stage: build
  parallel:
    matrix:
      - baseImage: [ alpine, debian ]
  variables:
    dotnetSdkVersion: 6.0.200
  script:
    - |
      rm .dockerignore
    - |
      docker build \
        --build-arg DOTNETSDK_VERSION=${dotnetSdkVersion} \
        --tag dd-trace-dotnet/${baseImage}-builder \
        --target releaser \
        --file "./tracer/build/_build/docker/${baseImage}.dockerfile" \
        .
    - |
      docker run \
        --env NugetPackageDirectory=/project/packages \
        --env tracerHome=/shared/bin/monitoring-home/tracer \
        --env artifacts=/project/tracer/src/bin/artifacts \
        --name release \
        dd-trace-dotnet/${baseImage}-builder \
        tracer/build.sh Clean BuildTracerHome ZipTracerHome
    - |
      docker cp release:/project/tracer/src/bin/artifacts/linux-x64 dist
  artifacts:
    paths:
      - dist/

# windows-build:
#   stage: build
#   tags:
#     - windows
#   script:
#     - tracer\build.ps1 Clean BuildTracerHome PackageTracerHome
#   artifacts:
#     paths:
#       - tracer/bin/artifacts/nuget/SignalFx.NET.Tracing.Azure.Site.Extension.*.nupkg
#       - tracer/bin/artifacts/*/en-us

sign-deb:
  stage: sign
  extends: .submit-request
  dependencies: 
    - linux-build
  before_script:
    - cp $(ls dist/*.deb) package.deb
  after_script:
    - mkdir dist/signed
    - mv -f tmp/package.deb "dist/signed/$(basename $(ls dist/*.deb))"
  variables:
    PROJECT: signalfx-dotnet-tracing
    ARTIFACT: package.deb
    SIGN_TYPE: DEB
    DOWNLOAD_DIR: tmp
    REPO_NAME: releng # this env var should be not needed in future
    REPO_PATH: signalfx-dotnet-tracing # this env var should be not needed in future
  artifacts:
    paths:
      - dist/signed/

sign-rpm:
  stage: sign
  extends: .submit-request
  dependencies: 
    - linux-build
  before_script:
    - cp $(ls dist/*.rpm) package.rpm
  after_script:
    - mkdir dist/signed
    - mv -f tmp/package.rpm "dist/signed/$(basename $(ls dist/*.rpm))"
  variables:
    PROJECT: signalfx-dotnet-tracing
    ARTIFACT: package.rpm
    SIGN_TYPE: RPM
    DOWNLOAD_DIR: tmp
    REPO_NAME: releng # this env var should be not needed in future
    REPO_PATH: signalfx-dotnet-tracing # this env var should be not needed in future
  artifacts:
    paths:
      - dist/signed/

checksums:
  stage: checksum
  dependencies: 
    - sign-deb
    - sign-rpm
  script:
    - pushd dist/signed && shasum -a 256 * > checksums.txt && popd
  artifacts:
    paths:
      - dist/

checksums-sign:
  stage: checksum-sign
  extends: .submit-request
  dependencies: 
    - checksums
  variables:
    PROJECT: signalfx-dotnet-tracing
    ARTIFACT: dist/signed/checksums.txt
    SIGN_TYPE: GPG
    DOWNLOAD_DIR: dist/signed
    REPO_NAME: releng # this env var should be not needed in future
    REPO_PATH: signalfx-dotnet-tracing # this env var should be not needed in future
  artifacts:
    paths:
      - dist/
